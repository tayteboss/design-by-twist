import styled from "styled-components";

const PartnersSectionWrapper = styled.section``;

const PartnersSection = () => {
  return <PartnersSectionWrapper>PartnersSection</PartnersSectionWrapper>;
};

export default PartnersSection;
